# TypeScript and Next.js Legacy Rule

There is no active Next.js frontend for the current Technical Office Suite target.

## Rules
- The active dashboard is `runtime/technical_office_runtime/static/index.html`.
- Do not create `apps/technical-office-web` unless the user changes the product target.
- Keep `apps/office3d` parked and isolated from runtime validation.
- If legacy Next.js work is explicitly requested, follow the local `apps/office3d` conventions and run tests only for that legacy task.
